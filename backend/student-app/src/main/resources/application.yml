server:
  port: 9200

#spring:
#  application:
#    name: STUDENT-SERVICE
#  datasource:
#    url: jdbc:mysql://localhost:3306/Student_db
#    username: lalith
#    password: lalith
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#    database: mysql
spring:
  application:
    name: SECURITY-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost:3306}/${DB_NAME:Student_db} #?useSSL=false
    username: ${DB_USERNAME:deepesh}
    password: ${DB_PASSWORD:deepesh}
    hikari:
      #      minimumIdle: 2
      #      maximumPoolSize: 10
      #      idleTimeout: 120000
      #      connectionTimeout: 300000
      #      leakDetectionThreshold: 300000
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:9000/eureka
#    instance:
#      hostname: localhost